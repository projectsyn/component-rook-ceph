= Alert rule: CephMonQuorumAtRisk

include::partial$runbooks/contribution_note.adoc[]

== icon:glasses[] Overview

This alert fires if two or fewer MONs are active in the Ceph cluster.
If the cluster is in this state, the Ceph cluster will lose the MON Quorum which is required to ensure data consistency, if one more MON becomes inactive.

== icon:bug[] Steps for debugging

:component: MON

include::partial$runbooks/check_component_pod_status.adoc[]
